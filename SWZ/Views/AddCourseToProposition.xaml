<Page
    x:Class="SWZ.Views.AddCourseToProposition"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SWZ.Views"
    xmlns:viewmodel="using:SWZ.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Orientation="Vertical">
            <Button Style="{StaticResource NavigationBackButtonNormalStyle}" Command="{x:Bind viewModel.GoBack}"/>
            <StackPanel Orientation="Horizontal" Margin="50">
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Wydział:" />
                    <TextBox HorizontalAlignment="Left" Margin="0,10" Text="TextBox" VerticalAlignment="Top" Height="30" Width="250"/>
                    <TextBlock Text="Kierunek studiów"/>
                    <TextBox HorizontalAlignment="Left" Margin="0,10" Text="TextBox" VerticalAlignment="Top" Height="30" Width="250"/>
                    <TextBlock Text="Kierunek studiów"/>
                    <StackPanel Orientation="Horizontal">
                        <RadioButton Content="Stacjonarne"></RadioButton>
                        <RadioButton Content="Niestacjonarne"></RadioButton>
                    </StackPanel>
                    <TextBlock Text="Język studiów"/>
                    <StackPanel Orientation="Horizontal">
                        <RadioButton Content="polski"></RadioButton>
                        <RadioButton Content="angielski"></RadioButton>
                    </StackPanel>
                </StackPanel>
                <StackPanel Margin="20,0,0,0">
                    <TextBlock Text="Kod kursu:" />
                    <TextBox HorizontalAlignment="Left" Margin="0,10" Text="TextBox" VerticalAlignment="Top" Height="30" Width="250"/>
                    <TextBlock Text="Nazwa kursu:"/>
                    <TextBox HorizontalAlignment="Left" Margin="0,10" Text="TextBox" VerticalAlignment="Top" Height="30" Width="250"/>
                    <TextBlock Text="Forma kursu:"/>
                    <ComboBox HorizontalAlignment="Stretch" 
                              VerticalAlignment="Stretch" 
                              x:Name="SearchCourseType"
                              SelectedIndex="{x:Bind viewModel.SelectedCourseType, Mode=TwoWay}">
                    </ComboBox>
                    


                </StackPanel>
            </StackPanel>
            <StackPanel Margin="50,0,50,0">
                <TextBlock Text="Kurs:"></TextBlock>
                <ListView x:Name="Courses" ItemsSource="{x:Bind viewModel.Courses}" Height="100" SelectedIndex="{x:Bind viewModel.SelectedCourseIndex, Mode=TwoWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Code}"></TextBlock>
                                <TextBlock Text="{Binding Name}"></TextBlock>
                                <TextBlock Text="{Binding ECTS}"></TextBlock>
                                <TextBlock Text="{Binding ZZU}"></TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>

                </ListView>
                <Button Content="Dodaj" Command="{x:Bind viewModel.AddCourse}"></Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
